<%= form_for(@cockpit) do |f| %>
  <% if @cockpit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cockpit.errors.count, "error") %> prohibited this cockpit from being saved:</h2>

      <ul>
      <% @cockpit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
