<%= form_for(@chunk_wall) do |f| %>
  <% if @chunk_wall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chunk_wall.errors.count, "error") %> prohibited this chunk_wall from being saved:</h2>

      <ul>
      <% @chunk_wall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
